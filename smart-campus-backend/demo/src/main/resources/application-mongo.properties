# Mongo profile for coursework / deployment.
# Activate with: SPRING_PROFILES_ACTIVE=mongo
# Optional: set MONGODB_URI for remote clusters.

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/smart_campus_db}